import React from 'react'

const Weather = (props) => {
    // console.log("Weather", props) 
    return (
        <>  
            {/* <h5> Zip: {props.user.weather && props.user.weather.zip} </h5> */}
            <h2> ⛅  88 ° </h2>
            <h5> Anthem, AZ </h5>
        </>
    )
}

export default (Weather)