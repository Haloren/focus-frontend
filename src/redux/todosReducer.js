const initialTodoState = {
    todos: []
}

const todosReducer = (state = initialTodoState, action) => {
    switch (action.type){

        default: 
            return {...state}
    }
}

export default todosReducer